#include<stdio.h>                                        // ไม่มีเครื่องหมาย # // ไม่มีเครื่องหมาย<>

int main () {                                          // ไม่มีเครื่องหมาย ;
    int a = 10;                                            // ไม่มีเครื่องหมาย ; // ไม่มีเครื่องหมาย \n 
    printf("a = %d\n", a); 
    return 0;                                          // ไม่มีเครื่องหมาย ;  
}                                                